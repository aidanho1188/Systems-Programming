#include <stdio.h>

int main(){
	// create a variable and set it
	int x;
	x = 10;

	// User the variable though an expression x / 2
	int y = x / 2;
	// What did we just do? We create an expression
	// statement
	// declearation
	// initialization


	// what is the value of y?	ans.5  y = 10/2
	printf("The value of Y is %d\n", y);


	return 0;
}
